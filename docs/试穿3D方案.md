# 3D 试穿方案 v1（已作废）

> **请勿再按本文实施。**  
> 旧方案（`tryon-xr` + 平面挂点 + `tryon3dModelFrame.js`）效果不达标，已停止维护。

## 请改用 v2

**[试穿3D-v2-shell方案.md](./试穿3D-v2-shell方案.md)**

- 体表壳网格（`shell_top` 等）+ 标准 UV + 动态 PNG  
- 当前试验：**仅 `shell_top`**  
- 衣橱「实时试穿」仍跳回首页  
- 仍使用 GLB（`avatar-female.glb` / `avatar-male.glb`）

## v1 遗留代码（待 v2 接入后删除）

| 模块 | 说明 |
|------|------|
| `components/tryon-xr/` | 平面贴图组件 |
| `utils/tryon3dModelFrame.js` | 旧缩放/bbox |
| `utils/tryon3dAnchors.js` | 旧挂点坐标 |

## 仍有效的部分

- `utils/tryonOutfitHelpers.js` / `tryonItemSlotsSync.js`  
- `commitTryonToHome` + 跳回首页  
- `utils/wardrobeItem.js`（srcFront / srcBack）  
- `components/model-tryon/`（2D Canvas，可作导出穿搭照等用途）
